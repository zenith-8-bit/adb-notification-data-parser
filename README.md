# adb-notification-data-parser
Python code parses raw adb shell dumpsys notification output into structured JSON/data, extracts titles, text, timestamps, channels, priorities, actions (buttons), and more
